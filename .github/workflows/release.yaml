name: release-prd
on:
  workflow_dispatch:
    inputs:
      increment:
        description: "increment"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  release-prd:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: git tag -l
      - run: go install github.com/suzuito/sandbox2-common-go/tools/release/cmd/increment-release-version@v0.0.1
      - run: echo $PATH
      - run: which git
      - run: increment-release-version -increment ${{ inputs.increment }} -branch ${{ github.ref }} -prefix v -git /bin/git -owner suzuito -repo sandbox2-common-go -token ${{ github.token }}
